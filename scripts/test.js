// const people = ["Mayowa", "Ekoh", "BossLady", "Bites", "Rahman", "Nath", "Barr"];

mapping(
    uint => mapping(
        address => bool
    )
) hasApproved;
hasApproved[uint] = mapping(address => bool);


"0x4567"
const hasApproved = {
    23: {
        "0xdead": true,
        "0x1234": true,
        "0x4567": true
    }
}

mapping(address => uint) oned
oned[address]


70 % of 5 = (70 / 100) * 5
=> (70 * 12) / 100
840 / 100 = 8.4 ~= 8